# Express Global Error Handling Example

This project demonstrates how to implement **global error handling** in an Express.js server, including handling:

- `404 Not Found` errors (routes that do not exist)
- `500 Internal Server Error` (server errors)

The server returns structured JSON responses for all errors.

---

## 📦 Project Setup

### 1. Clone the repository
```bash
git clone <your-repo-link>
cd <your-repo-folder>
